# Procedural Spell Tree Builder Dependencies
# Install with: pip install -r requirements.txt

scikit-learn>=1.3.0      # TF-IDF vectorization for theme discovery
thefuzz>=0.22.1          # Fuzzy string matching for spell grouping
python-Levenshtein>=0.25 # Fast Levenshtein distance (speeds up thefuzz)
requests>=2.31.0         # HTTP client for LLM API calls (optional)
networkx>=3.0            # Graph algorithms for arborescence tree builder (graph mode)
